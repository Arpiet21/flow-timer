{
  "version": 2,
  "buildCommand": null,
  "installCommand": "npm install --omit=dev",
  "outputDirectory": ".",
  "framework": null,
  "routes": [
    { "src": "/", "dest": "/landing.html" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
